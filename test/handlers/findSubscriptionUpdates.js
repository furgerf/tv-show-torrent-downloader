'use strict';

/*
var rewire = require('rewire'),
  findSubscriptionUpdates = rewire('../../src/endpoints/findSubscriptionUpdates');

describe('endpoints/findSubscriptionUpdates - getTorrentSort', function () {
  var getTorrentSort = findSubscriptionUpdates.__get__('getTorrentSort');

  it('should return the correct known sort with valid input', function () {
    expect(getTorrentSort('largest')).toBe('largest');
    expect(getTorrentSort('LARGEST')).toBe('largest');
    expect(getTorrentSort('LaRgEsT')).toBe('largest');

    expect(getTorrentSort('SmAlLeSt')).toBe('smallest');

    expect(getTorrentSort('NeWeSt')).toBe('newest');

    expect(getTorrentSort('OlDeSt')).toBe('oldest');
  });

  it('should return the default sort with invalid input', function () {
    var defaultSort = 'largest';

    expect(getTorrentSort(undefined)).toBe(defaultSort);
    expect(getTorrentSort(null)).toBe(defaultSort);
    expect(getTorrentSort(1234)).toBe(defaultSort);
    expect(getTorrentSort({})).toBe(defaultSort);
    expect(getTorrentSort('foobar')).toBe(defaultSort);
  });
});

describe('endpoints/findSubscriptionUpdates', function () {
  it('SHOULD BE IMPLEMENTED (FRISBY TESTS)', function () {
    expect(1).toBe(1);
  });
});
*/

